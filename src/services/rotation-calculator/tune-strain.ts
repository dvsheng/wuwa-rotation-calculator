/**
 * Sentinel ID for the virtual "Tune Strain" buff that lets users apply tune strain
 * stacks to the enemy.
 *
 * This buff is shown in the "Other" section of the buff palette when any character
 * on the team has a permanent_stat with stat tuneStrainDamageBonus.
 */
export const TUNE_STRAIN_BUFF_ID = -1000;
